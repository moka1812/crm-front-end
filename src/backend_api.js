const baseURL = "http://127.0.0.1:8000/api"

export const loginApi = `${baseURL}/auth/token/`
export const refreshTokenApi = `${baseURL}/auth/token/refresh/`

export const createOrderApi = `${baseURL}/create_order/`
